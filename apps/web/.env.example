# Base URL of the API. Leave unset when the API also serves this frontend, which
# is the single-container self-hosted setup. Set it when the SPA is hosted
# separately, for example on a CDN.
# NEXT_PUBLIC_API_BASE_URL=https://api.example.com
